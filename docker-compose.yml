# in development
backend:
  image: node:5.4-onbuild
  ports:
   - "3000:3000"
  volumes:
   - ../family-tree-backend:/code
  working_dir: "/code"
  links:
    - mongodb
mongodb:
  image: mongo:3.2
  ports:
   - "27017:27017"
